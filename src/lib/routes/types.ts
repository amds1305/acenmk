
import { RouteMetadata } from '@/types/permissions';

// Re-export the RouteMetadata type for convenience
export type { RouteMetadata } from '@/types/permissions';

// Type for route metadata maps
export type RouteMetadataMap = Record<string, RouteMetadata>;
