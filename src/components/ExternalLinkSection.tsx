
// This component is used for rendering external links as sections
import React from 'react';

const ExternalLinkSection: React.FC = () => {
  return null; // External links are handled at the routing level
};

export default ExternalLinkSection;
